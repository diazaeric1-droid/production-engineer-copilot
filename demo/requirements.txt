# Streamlit Cloud uses this file (closer to demo/app.py wins over root requirements.txt).
# Kept in sync with the root requirements.txt for local devs who prefer running from repo root.
streamlit>=1.31.0
plotly>=5.18.0
anthropic>=0.40.0
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.7.0
